# Core API Dependencies
google-generativeai==0.8.3
python-dotenv==1.0.1

# HTTP and JSON handling (built-in modules, listed for reference)
# os, json, urllib.parse, traceback, http.server - these are built-in Python modules

# Vercel Serverless Runtime Support
vercel-python-runtime==0.1.0
